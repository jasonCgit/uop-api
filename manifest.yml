---
applications:
  - name: uop-api
    memory: 512M
    instances: 2
    buildpack: python_buildpack
    command: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    health-check-type: http
    health-check-http-endpoint: /api/health-summary
    env:
      USE_MOCK_DATA: "true"
